import 'package:flutter/material.dart';

class NewWordsView extends StatelessWidget {
  const NewWordsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
